<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:berber_randevu_uygulamasi.Views"
    xmlns:models="clr-namespace:berber_randevu_uygulamasi.Models"
    xmlns:altbar="clr-namespace:berber_randevu_uygulamasi.Views.AltBarlar"
    x:Class="berber_randevu_uygulamasi.Views.BerberCalisanlarSayfasi"
    Title="Çalışanlar"
    BackgroundColor="#0F0F0F">

    <Grid RowDefinitions="Auto,*,Auto">
        <Grid Padding="12,10" ColumnDefinitions="*,Auto">
            <Label Text="Çalışanlar" FontSize="20" FontAttributes="Bold" TextColor="#F2F2F2"/>
            <Button Grid.Column="1" Text="＋" Clicked="Ekle_Clicked"/>
        </Grid>

        <CollectionView Grid.Row="1" x:Name="listeCalisan">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:CalisanKart">
                    <Border BackgroundColor="#1A1A1A" Stroke="#2B2B2B" StrokeShape="RoundRectangle 18" Padding="14" Margin="12,8">
                        <Grid ColumnDefinitions="Auto,*,Auto" RowDefinitions="Auto,Auto">
                            <Border BackgroundColor="#232323" StrokeShape="RoundRectangle 14" WidthRequest="44" HeightRequest="44">
                                <Label Text="👤" FontSize="18" HorizontalOptions="Center" VerticalOptions="Center"/>
                            </Border>

                            <VerticalStackLayout Grid.Column="1" Margin="12,0,0,0" Spacing="2">
                                <Label Text="{Binding Ad}" FontAttributes="Bold" TextColor="#F2F2F2"/>
                                <Label Text="{Binding Rol}" FontSize="12" TextColor="#BDBDBD"/>
                                <Label Text="{Binding Telefon}" FontSize="12" TextColor="#BDBDBD"/>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="2" Spacing="6">
                                <Button Text="Çalışanı Sil" Clicked="CalisaniSil_Clicked" CommandParameter="{Binding}"/>
                            </VerticalStackLayout>

                            <Label Grid.Row="1" Grid.ColumnSpan="3"
                                   Text="{Binding Durum}"
                                   FontSize="12" TextColor="#BDBDBD"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <altbar:BerberAltBar Grid.Row="2"/>
    </Grid>
</ContentPage>
